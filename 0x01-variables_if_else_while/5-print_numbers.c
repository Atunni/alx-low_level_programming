#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * return: Returns 0
 */

int main(void)

{
	int x;

	for (x = 0; x < 9; x++)

	{
		printf("%d\n", x);
	}

	return (0);
}

