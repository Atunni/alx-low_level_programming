#include "main.h"

/**
 * _abs - prints function that computes the absolute value of an integer
 *
 *@n:n
 *
 * Return: Returns int
 */

int _abs(int n)
{
	int x;

	if (n >= 0)
		return (x);

	else
		return (-x);

}
